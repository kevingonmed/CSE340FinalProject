<%- include('partials/header') %>
    <section class="hero">
        <p class="hero-kicker">AI-powered travel planner</p>
        <h1>Stop overthinking. Start wandering.</h1>
        <p>WanderDecide helps you choose a destination and generate a day-by-day itinerary that matches your dates, budget, group, and vibe.</p>
        <% if (isLoggedIn) { %>
            <a href="/trips/new" class="btn">Plan a Trip</a>
            <% } else { %>
                <a href="/register" class="btn">Get Started</a>
                <a href="/login" class="btn btn-outline">Login</a>
                <% } %>
    </section>
    <%- include('partials/footer') %>